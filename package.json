{
  "name": "seoul-archive",
  "private": true,
  "type": "module",
  "scripts": {
    "generate:data": "node scripts/generate-data.mjs",
    "build": "exit 0",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.78.0"
  }
}

